from django.conf.urls import url
from . import views

urlpatterns = [
    url(r'^$', views.home, name='home'),
    url(r'^ciao$', views.index, name='index'),
    url(r'^search$',views.search, name='search')
    url(r'^login$',views.login,name=login)
]
